fn main() {
    let names = vec!["mamad", "ali", "reza", "sara"];
    let numbers: Vec<i32> = Vec::new();
    println!("names is empty: {}", names.is_empty());
    println!("numbers is empty: {}", numbers.is_empty());
}
